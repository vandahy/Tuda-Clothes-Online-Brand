[phases.setup]
nixPkgs = ['jdk17', 'maven']

[phases.build]
cmds = ['mvn clean package -DskipTests']

[start]
cmd = 'java -Dserver.port=$PORT -jar target/demo-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod'
